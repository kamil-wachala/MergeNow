<UserControl x:Class="MergeNow.Views.MergeNowSectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vms="clr-namespace:MergeNow.ViewModels"
             xmlns:shell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:platform="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="320" d:DesignWidth="400"
             IsEnabled="{Binding IsSectionEnabled}">

    <d:UserControl.DataContext>
        <vms:MergeNowSectionDesignViewModel/>
    </d:UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="VsTextBlockStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ToolWindowTextBrushKey}}" />
            </Style>
            <Style x:Key="VsHyperlinkStyle" TargetType="Hyperlink">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.PanelHyperlinkBrushKey}}" />
                <Setter Property="TextDecorations" Value="None" />
            </Style>
            <Style x:Key="VsExpanderStyle" TargetType="Expander">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ToolWindowTextBrushKey}}" />
                <Setter Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
                <Setter Property="BorderThickness" Value="0" />
            </Style>
            <Style x:Key="BranchPickerButtonStyle" TargetType="ToggleButton">
                <Setter Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxBackgroundBrushKey}}" />
                <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxTextBrushKey}}" />
                <Setter Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxBorderBrushKey}}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="5,0,0,0" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="OuterBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="17" />
                                    </Grid.ColumnDefinitions>

                                    <ContentPresenter Grid.Column="0"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />

                                    <Border x:Name="GlyphBorder"
                                            Grid.Column="1"
                                            Background="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxBackgroundBrushKey}}"
                                            BorderBrush="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxBorderBrushKey}}"
                                            BorderThickness="1,0,0,0">
                                        <Path x:Name="Glyph"
                                              Width="7"
                                              Height="4"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Data="M 0 0 L 3.5 4 L 7 0 Z"
                                              Fill="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxGlyphBrushKey}}"
                                              Stretch="Fill" />
                                    </Border>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxDisabledBackgroundBrushKey}}" />
                                    <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxDisabledBorderBrushKey}}" />
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxDisabledTextBrushKey}}" />
                                    <Setter TargetName="GlyphBorder" Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxDisabledBackgroundBrushKey}}" />
                                    <Setter TargetName="GlyphBorder" Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxDisabledBorderBrushKey}}" />
                                    <Setter TargetName="Glyph" Property="Fill" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxDisabledGlyphBrushKey}}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxMouseOverBackgroundGradientBrushKey}}" />
                                    <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxMouseOverBorderBrushKey}}" />
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxMouseOverTextBrushKey}}" />
                                    <Setter TargetName="GlyphBorder" Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxButtonMouseOverBackgroundBrushKey}}" />
                                    <Setter TargetName="GlyphBorder" Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxButtonMouseOverSeparatorBrushKey}}" />
                                    <Setter TargetName="Glyph" Property="Fill" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxMouseOverGlyphBrushKey}}" />
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxFocusedBackgroundBrushKey}}" />
                                    <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxFocusedBorderBrushKey}}" />
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxFocusedTextBrushKey}}" />
                                    <Setter TargetName="GlyphBorder" Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxFocusedButtonBackgroundBrushKey}}" />
                                    <Setter TargetName="GlyphBorder" Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxFocusedButtonSeparatorBrushKey}}" />
                                    <Setter TargetName="Glyph" Property="Fill" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxFocusedGlyphBrushKey}}" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxMouseDownBackgroundBrushKey}}" />
                                    <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxMouseDownBorderBrushKey}}" />
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxMouseDownTextBrushKey}}" />
                                    <Setter TargetName="GlyphBorder" Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxButtonMouseDownBackgroundBrushKey}}" />
                                    <Setter TargetName="GlyphBorder" Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxButtonMouseDownSeparatorBrushKey}}" />
                                    <Setter TargetName="Glyph" Property="Fill" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxMouseDownGlyphBrushKey}}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="BranchPickerListStyle" TargetType="ListBoxItem">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxItemTextBrushKey}}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="6,4" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border x:Name="ItemBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxItemMouseOverBackgroundBrushKey}}" />
                                    <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxItemMouseOverBorderBrushKey}}" />
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxItemMouseOverTextBrushKey}}" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxSelectionBrushKey}}" />
                                    <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxFocusedBorderBrushKey}}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsSelected" Value="True" />
                                        <Condition Property="Selector.IsSelectionActive" Value="False" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxItemTextInactiveBrushKey}}" />
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="BranchPickerListBoxStyle" TargetType="ListBox">
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxPopupBackgroundGradientBrushKey}}" />
                <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxItemTextBrushKey}}" />
                <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
                <Setter Property="Visibility" Value="Visible" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding AnyFilteredTargetBranches}" Value="False">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="BranchPickerPopupBorderStyle" TargetType="Border">
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxPopupBorderBrushKey}}" />
                <Setter Property="Background" Value="{DynamicResource {x:Static platform:EnvironmentColors.ComboBoxPopupBackgroundGradientBrushKey}}" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Changeset" Margin="0,10,0,0" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"
                Style="{StaticResource VsTextBlockStyle}"/>
        <TextBox Margin="5,10,0,0" Height="22" Grid.Row="0" Grid.Column="1" x:Name="ChangesetNumberTb"
                Text="{Binding ChangesetNumber, UpdateSourceTrigger=PropertyChanged}"
                Style="{DynamicResource {x:Static shell:VsResourceKeys.TextBoxStyleKey}}"/>
        <TextBlock Margin="5,10,0,0" Height="22" Grid.Row="0" Grid.Column="1" IsHitTestVisible="False" Opacity="0.5"
                   Text="Enter a changeset number" VerticalAlignment="Center" HorizontalAlignment="Left" Padding="3">
            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource VsTextBlockStyle}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSectionEnabled}" Value="True" />
                                <Condition Binding="{Binding Text, ElementName=ChangesetNumberTb}" Value="" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Button Content="..." Margin="5,10,1,0" VerticalAlignment="Center" Width="30" Height="22" Grid.Row="0" Grid.Column="2"
                Command="{Binding BrowseCommand}"
                Style="{DynamicResource {x:Static shell:VsResourceKeys.ButtonStyleKey}}"/>

        <Button Content="Find" Margin="5,5,0,0" HorizontalAlignment="Left" Width="120" Height="22" Grid.Row="1" Grid.Column="1"
                Grid.ColumnSpan="2" Command="{Binding FindCommand}"
                Style="{DynamicResource {x:Static shell:VsResourceKeys.ButtonStyleKey}}"/>

        <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="5,5,5,0" Style="{StaticResource VsTextBlockStyle}">
            <Hyperlink Command="{Binding OpenChangesetCommand}" Style="{StaticResource VsHyperlinkStyle}">
                <TextBlock Text="{Binding ChangesetName}" TextWrapping="Wrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="TextDecorations" Value="Underline" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Hyperlink>
        </TextBlock>

        <TextBlock Text="Merge To" Margin="0,10,0,0" VerticalAlignment="Center" Grid.Row="3" Grid.Column="0"
                Style="{StaticResource VsTextBlockStyle}"/>
        <Grid Margin="5,10,1,0" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2">
            <ToggleButton x:Name="TargetBranchPickerButton"
                          Height="22"
                          IsChecked="{Binding IsTargetBranchPickerOpen, Mode=TwoWay}"
                          IsEnabled="{Binding AnyTargetBranches}"
                          Style="{StaticResource BranchPickerButtonStyle}">
                <TextBlock VerticalAlignment="Center"
                           Text="{Binding TargetBranchPickerDisplayText}"
                           TextTrimming="CharacterEllipsis" />
            </ToggleButton>

            <Popup IsOpen="{Binding IsTargetBranchPickerOpen, Mode=TwoWay}"
                   Placement="Bottom"
                   PlacementTarget="{Binding ElementName=TargetBranchPickerButton}"
                   StaysOpen="False"
                   Opened="TargetBranchPopup_OnOpened">
                <Border Width="{Binding ActualWidth, ElementName=TargetBranchPickerButton}"
                        Style="{StaticResource BranchPickerPopupBorderStyle}">
                    <StackPanel FocusManager.FocusedElement="{Binding ElementName=TargetBranchSearchTb}">
                        <Grid Margin="4">
                            <TextBox x:Name="TargetBranchSearchTb"
                                     Text="{Binding TargetBranchSearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource {x:Static shell:VsResourceKeys.TextBoxStyleKey}}"/>
                            <TextBlock IsHitTestVisible="False"
                                       Margin="4,0,0,0"
                                       VerticalAlignment="Center"
                                       Opacity="0.5"
                                       Text="Search branches">
                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource VsTextBlockStyle}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=TargetBranchSearchTb}" Value="">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>

                        <ListBox MaxHeight="180"
                                 ItemsSource="{Binding FilteredTargetBranches}"
                                 SelectedItem="{Binding SelectedTargetBranch, Mode=TwoWay}"
                                 Style="{StaticResource BranchPickerListBoxStyle}"
                                 ItemContainerStyle="{StaticResource BranchPickerListStyle}" />

                        <TextBlock Margin="8,4,8,8"
                                   Text="No matching branches">
                            <TextBlock.Style>
                                <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource VsTextBlockStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AnyFilteredTargetBranches}" Value="False">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </Popup>
        </Grid>

        <Button Content="Merge" Width="120" Margin="5,5,5,0" Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"
                Height="22" Command="{Binding MergeCommand}" HorizontalAlignment="Left"
                Style="{DynamicResource {x:Static shell:VsResourceKeys.ButtonStyleKey}}"/>

        <Expander Header="Advanced" Margin="5,10,5,0" Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                  IsExpanded="{Binding IsAdvancedExpanded}"  Style="{StaticResource VsExpanderStyle}">
            <StackPanel Orientation="Vertical" Margin="5">

                <CheckBox Margin="0,5,0,0" Content="Combined merge" VerticalAlignment="Center"
                    IsChecked="{Binding CombinedMerge}" ToolTipService.InitialShowDelay = "100"
                    Style="{DynamicResource {x:Static shell:VsResourceKeys.CheckBoxStyleKey}}">
                    <CheckBox.ToolTip>
                        <TextBlock>
                            Select when you want to inlude several merges to different target branches in one changeset.
                            <LineBreak />
                            The changeset comment will be amended not replaced.
                        </TextBlock>
                    </CheckBox.ToolTip>
                </CheckBox>

                <WrapPanel Orientation="Horizontal">
                    <Button Margin="0,10,10,0" Content="Clear Pending Changes" Height="22"
                        Command="{Binding ClearPageCommand}" HorizontalAlignment="Left" ToolTipService.InitialShowDelay = "100"
                        Style="{DynamicResource {x:Static shell:VsResourceKeys.ButtonStyleKey}}">
                        <Button.ToolTip>
                            <TextBlock>
                                Clears comment, clears associated workitems and excludes all pending changes.
                                <LineBreak />
                                This will not undo your pending changes.
                            </TextBlock>
                        </Button.ToolTip>
                    </Button>

                    <Button Margin="0,10,0,0" Content="Clear Merge Now" Height="22" ToolTip="Clears Merge Now section."
                        Command="{Binding ClearMergeNowCommand}" HorizontalAlignment="Left" ToolTipService.InitialShowDelay = "100"
                        Style="{DynamicResource {x:Static shell:VsResourceKeys.ButtonStyleKey}}">
                    </Button>
                </WrapPanel>

            </StackPanel>
        </Expander>

    </Grid>
</UserControl>
